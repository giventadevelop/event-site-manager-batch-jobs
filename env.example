# Batch Jobs Application Environment Variables Example
# Copy this file to .env and update with your actual values
# This file is used for both local development and Docker deployment

# ============================================
# Server Configuration
# ============================================
SERVER_PORT=8081

# ============================================
# Database Configuration
# ============================================
RDS_ENDPOINT=localhost
DB_NAME=event_site_manager_db
DB_USERNAME=event_site_app
DB_PASSWORD=your-database-password
DB_MAX_POOL_SIZE=10
DB_MIN_IDLE=2

# ============================================
# AWS Configuration
# ============================================
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_REGION=us-east-2
AWS_SES_FROM_EMAIL=noreply@example.com
AWS_SES_RATE_LIMIT_PER_SECOND=200
AWS_S3_BUCKET_NAME=your-s3-bucket-name

# ============================================
# Stripe Configuration (optional)
# Default API key - tenant-specific keys are loaded from database
# ============================================
STRIPE_DEFAULT_API_KEY=your-stripe-default-api-key

# ============================================
# Batch Job Configuration (optional - defaults provided)
# ============================================
SUBSCRIPTION_RENEWAL_ENABLED=true
SUBSCRIPTION_RENEWAL_CRON=0 0 */6 * * *
SUBSCRIPTION_RENEWAL_BATCH_SIZE=100
SUBSCRIPTION_RENEWAL_MAX_SUBSCRIPTIONS=10000
SUBSCRIPTION_RENEWAL_DAYS_BEFORE=7

EMAIL_BATCH_ENABLED=true
EMAIL_BATCH_CRON=0 0 2 * * *
EMAIL_BATCH_SIZE=50
EMAIL_BATCH_MAX_EMAILS=10000

# ============================================
# Notes
# ============================================
# The following variables from backend are NOT needed for batch jobs:
# - CLERK_* (authentication - not used in batch jobs)
# - JWT_* (authentication - not used in batch jobs)
# - TWILIO_* (WhatsApp - not used in batch jobs)
# - EMAIL_HOST_URL_PREFIX (frontend URL - not used in batch jobs)
# - QRCODE_SCAN_HOST_PREFIX (frontend URL - not used in batch jobs)
# - PAYMENT_ENCRYPTION_KEY (payment processing - Stripe keys come from database)
#
# For Docker deployment, these environment variables are automatically
# passed to the container. No OS-specific path configuration is needed.




