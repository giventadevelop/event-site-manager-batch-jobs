# Batch Jobs Application Environment Variables Example
# Copy this file to .env and update with your actual values

# Server Configuration
SERVER_PORT=8081

# Database Configuration
RDS_ENDPOINT=localhost
DB_NAME=event_site_manager_db

DB_MAX_POOL_SIZE=10
DB_MIN_IDLE=2

# AWS Configuration (for SES email sending)

# Note: The following variables from backend are NOT needed for batch jobs:
# - CLERK_* (authentication - not used in batch jobs)
# - JWT_* (authentication - not used in batch jobs)
# - TWILIO_* (WhatsApp - not used in batch jobs)
# - EMAIL_HOST_URL_PREFIX (frontend URL - not used in batch jobs)
# - QRCODE_SCAN_HOST_PREFIX (frontend URL - not used in batch jobs)
# - PAYMENT_ENCRYPTION_KEY (payment processing - Stripe keys come from database)


