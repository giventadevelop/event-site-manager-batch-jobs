# GitHub Personal Access Token (for sync-env-vars-to-github.js)
# Create at: GitHub → Settings → Developer settings → Personal access tokens
# Classic PAT: required scope "repo". Fine-grained: grant "Actions" read/write for the repo.
# Use the raw token only (no quotes). Example: GIT_PAT_TOKEN=ghp_xxxxxxxxxxxx
GIT_PAT_TOKEN=
